# 参与贡献

欢迎提交问题、修正文档和改进代码。项目围绕本机会议中的原话、讨论与人工确认展开；涉及行为变化时，请说明具体使用场景。

## 本地开发

准备 Node.js 22.13+、npm 和 ffmpeg，在项目目录运行：

```sh
npm ci
npm run dev
```

网页使用 `http://127.0.0.1:5187`，API 使用 `http://127.0.0.1:8797`。试用完整虚构会议可先执行 `npm run build`，再执行 `npm run demo`。配置真实模型服务前请阅读[上手指南](docs/GETTING_STARTED.md)。

## 提交问题

在仓库 Issues 中说明系统、Node.js 版本、复现步骤、期望结果和实际结果。涉及录音或 AI 时，分开描述音频保存、转录和分析的状态。优先提供最小的虚构示例。

不要上传 `.env`、`data/`、完整数据库、真实会议录音或未经核对的日志。发现安全问题时，按 [SECURITY.md](SECURITY.md) 私下报告。

## 提交代码

1. Fork 仓库，建立分支，一次 PR 处理一个明确问题。
2. 说明变化的触发条件、结果和影响；界面变化附截图，示例内容使用虚构数据。
3. 为行为变化补充有针对性的测试。仅修改文案或图片时，检查链接与渲染即可。
4. 提交前执行 `npm run release:check`，在 PR 中注明结果及未完成的验证。

自动检查不需要云服务密钥。真实 ASR / LLM、麦克风权限、系统声音和声纹效果需要另外试用，不能用模拟测试代替。验证步骤见 [docs/VALIDATION.md](docs/VALIDATION.md)。

## 实现约定

- 页面与 MCP 共用业务 API，避免产生两套行为。
- AI 结果保留原文引用和来源版本；人工修正不被后台结果静默覆盖。
- 记录讨论进展不代表形成共识；退出焦点不代表未完成的工作已经完成。
- 新增测试、截图和演示数据必须完全虚构；不要将真实转录改名后作为公开样例。
- 本地数据和凭证留在 Git 忽略的目录。提交前用 `git diff --cached` 检查实际提交内容。

贡献内容沿用本项目的 Apache-2.0 许可证。引入第三方代码或素材时保留其许可证和来源说明。

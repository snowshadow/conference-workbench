# 安全与隐私

## 使用边界

会议工作台用于单台电脑、单主持人使用。服务只接受回环地址与允许的本机网页来源，没有多用户账户或公网服务所需的认证机制。请勿通过端口转发、隧道或反向代理将它暴露到公网。

`data/` 包含会议、转录、录音、声音样本、模型设置和密钥。数据库文件并未加密；本机文件权限限制不能替代设备与备份保护。`.env` 同样可能包含凭证。

远程语音识别服务接收音频，远程大模型服务接收相关转录与上下文。录音前取得参会者知情同意，按实际需求选择服务和数据处理方式。项目不附带云端账户。

## 报告漏洞

不要在公开 Issue 中粘贴可用密钥、真实会议或包含私人数据的复现包。

优先使用仓库 **Security → Report a vulnerability** 私下报告。若仓库尚未启用此入口，可先发不含漏洞细节或私人数据的 Issue，请维护者提供私密报告渠道。

报告中请提供受影响版本、触发条件、预期与实际结果，以及使用虚构数据的最小复现。项目目前没有承诺固定的安全响应时限。

## 分享与发布

分享程序只包含源代码、公开文档和虚构示例。不要打包自己的工作目录：忽略规则不会自动过滤手工 ZIP，也不会清除旧 Git 历史。

如果数据曾进入 Git 提交，仅修改或忽略当前文件不能消除历史副本。首次开源前应核对所有将公开的提交，或使用经过核对、不包含旧历史的源码重新初始化仓库。
